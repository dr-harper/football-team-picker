{
  "name": "gemini-proxy",
  "version": "1.0.0",
  "description": "Firebase Function proxy for Gemini API — keeps the API key server-side",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0"
  }
}
