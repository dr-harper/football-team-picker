# ── AI Proxy (recommended) ──────────────────────────────────────────
# URL of your Google Cloud Function (or other serverless proxy).
# When set, the Gemini API key stays server-side and never reaches the client.
VITE_AI_PROXY_URL=https://REGION-PROJECT.cloudfunctions.net/geminiProxy

# ── Direct key (fallback) ──────────────────────────────────────────
# Only used when VITE_AI_PROXY_URL is NOT set.
# The key is XOR-obfuscated in the bundle but still extractable — use
# the proxy for real security.
VITE_GEMINI_KEY=your-gemini-api-key-here
